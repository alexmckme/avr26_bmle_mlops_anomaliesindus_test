# ── Récupération du dataset (étape 0) ─────────────────────
kaggle>=1.6.0             # téléchargement MVTec AD via scripts/download_data.py

# ── Ingestion / stockage (MinIO) ───────────────────────────
minio>=7.2.0
python-dotenv>=1.0.0
Pillow>=11.0.0

# ── PaDiM (détection d'anomalies) ──────────────────────────
# NB : TF stable ne supporte pas encore Python 3.14 -> venv en Python 3.12.
# Multi-arch : `tensorflow-cpu` n'existe qu'en x86_64 ; sur linux/arm64
# (Docker sur Apple Silicon) on prend le wheel `tensorflow` complet.
tensorflow-cpu>=2.21.0; sys_platform == "linux" and platform_machine == "x86_64"
tensorflow>=2.21.0; sys_platform == "linux" and platform_machine == "aarch64"
tensorflow>=2.21.0; sys_platform != "linux"
scikit-learn>=1.5.0

# ── API (FastAPI) ──────────────────────────────────────────
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9   # pour les uploads de fichiers (UploadFile/Form)

# ── Suivi d'expériences / Model Registry ──────────────────
mlflow>=2.16.0
boto3>=1.34.0             # client S3 utilisé par MLflow pour écrire dans MinIO
